// Layout uden PraxisOS-chrome — simulerer at det er bypilar.dk's eget hostede website.
export default function FakeBypilarSiteLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
