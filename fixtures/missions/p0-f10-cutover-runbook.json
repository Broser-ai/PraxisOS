{
  "id": "p0-f10-cutover-runbook",
  "title": "P0 F10 · cutover runbook + memory/JSON import script (no execution)",
  "goal": "Cutover runbook (docs/ops/p0-db-cutover-runbook.md) + memory→PG import script (scripts/migrate-memory-to-pg.ts) that defaults to --dry-run (no DB writes). The cutover itself is NOT executed — Michael performs it manually on Hetzner. NO_AUTO_MERGE / NO_AUTO_DEPLOY / suggestion_only preserved.",
  "riskLevel": "yellow",
  "platformScope": ["clinic_ops", "prime"],
  "acceptanceCriteria": [
    { "text": "scripts/migrate-memory-to-pg.ts buildImportPlan emits INSERT INTO journal_entries/journals resolving tenant_id by slug (no hardcoded UUIDs)" },
    { "text": "import script defaults to --dry-run; --execute requires SUPABASE_URL + SERVICE_ROLE_KEY" },
    { "text": "runbook documents steps 0-7, NO_AUTO_MERGE, manual cutover, and blockers requiring Broser" },
    { "text": "vitest covers buildImportPlan (pure) + script/runbook presence; tsc green" }
  ],
  "allowedPaths": [
    "scripts/migrate-memory-to-pg.ts",
    "docs/ops/p0-db-cutover-runbook.md",
    "tests/f10-cutover-runbook.test.ts",
    "tests/prime/p0-slice-missions.test.ts",
    "fixtures/missions/p0-f10-cutover-runbook.json"
  ],
  "forbiddenPaths": [
    ".env",
    ".env.production",
    "docker-compose.praxis.yml",
    "supabase/migrations/",
    "app/api/"
  ],
  "workstreams": [
    { "role": "scout", "title": "Scout · memory/JSON durable sources" },
    { "role": "builder", "title": "Builder · import script + runbook" },
    { "role": "verifier", "title": "Verifier · f10-cutover-runbook tests" },
    { "role": "reviewer", "title": "Reviewer · cutover safety + rollback" }
  ]
}
